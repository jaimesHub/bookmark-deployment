# Secrets — TUYỆT ĐỐI không commit
.env
.env.local
.env.*.local

# Docker compose overrides (cho local dev)
docker-compose.override.yml

# OS / IDE
.DS_Store
.idea/
.vscode/

# Logs nếu lỡ tạo
*.log

# ===== Lec-6: RSA keys (secret material — generate trên VM, KHÔNG commit) =====
keys/private.pem
keys/public.pem
# Giữ placeholder nếu cần (vd: README trong keys/ giải thích structure)
!keys/.gitkeep
!keys/README.md
